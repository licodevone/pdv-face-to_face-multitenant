Glass surface panel — the container for nearly every grouped region in the PDV.

```jsx
<Card>
  <p className="eyebrow">Resumo</p>
  <h3>Caixa principal</h3>
</Card>
<Card soft>Conteúdo aninhado</Card>
```

`soft` switches to the nested surface tone without the ambient shadow.
